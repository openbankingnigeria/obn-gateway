import { ConfirmActionProps } from '@/types/webappTypes/appTypes'
import React from 'react'

const ApproveConsumer = ({
  close,
  loading,
  next,
}: ConfirmActionProps) => {
  return (
    <div>ApproveConsumer</div>
  )
}

export default ApproveConsumer